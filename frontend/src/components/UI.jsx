import { Loader2, Sparkles } from 'lucide-react';
export const Loading = ({ label = 'Preparing your workspace…' }) => <div className="flex min-h-[260px] flex-col items-center justify-center gap-4"><Loader2 className="animate-spin text-wine" size={34}/><p className="text-sm text-stone-500">{label}</p></div>;
export const Empty = ({ title = 'Nothing here yet', text = 'Create your first wedding to begin planning.' }) => <div className="card p-10 text-center"><Sparkles className="mx-auto mb-4 text-gold"/><h3 className="font-display text-xl">{title}</h3><p className="mx-auto mt-2 max-w-sm text-sm text-stone-500">{text}</p></div>;
export const Status = ({ value = 'planning' }) => <span className={`status ${value === 'completed' || value === 'approved' ? 'bg-emerald-100 text-emerald-700' : value === 'under_review' ? 'bg-amber-100 text-amber-700' : value === 'in_production' ? 'bg-sky-100 text-sky-700' : 'bg-blush text-wine'}`}>{value.replaceAll('_', ' ')}</span>;
export const StatCard = ({ icon: Icon, label, value, note }) => <div className="card p-5"><div className="flex items-start justify-between"><div><p className="text-sm text-stone-500">{label}</p><p className="mt-2 font-display text-3xl">{value}</p></div><div className="rounded-2xl bg-blush p-3 text-wine"><Icon size={22}/></div></div>{note && <p className="mt-3 text-xs text-stone-400">{note}</p>}</div>;
