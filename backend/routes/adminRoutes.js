import { Router } from 'express'; import { protect, authorize } from '../middleware/auth.js'; import * as c from '../controllers/adminController.js'; const router = Router(); router.use(protect, authorize('admin')); router.get('/dashboard', c.dashboard); router.get('/weddings', c.weddings); router.get('/users', c.users); router.put('/weddings/:id/status', c.updateStatus); router.put('/weddings/:id/assign-editor', c.assignEditor); export default router;
