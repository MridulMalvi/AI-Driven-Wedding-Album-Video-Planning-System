import { Router } from 'express'; import { protect } from '../middleware/auth.js'; import * as c from '../controllers/planController.js'; const router = Router(); router.use(protect); router.get('/weddings/:weddingId/video-plans', c.listVideoPlans); router.get('/video-plans/:id', c.getVideoPlan); router.put('/video-plans/:id', c.updateVideoPlan); router.get('/weddings/:weddingId/highlight', c.getHighlight); router.put('/highlight/:id', c.updateHighlight); router.get('/weddings/:weddingId/album-design', c.getAlbum); router.put('/album-design/:id', c.updateAlbum); export default router;
